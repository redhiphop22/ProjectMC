namespace common;

enum RESULT_CODE : uint32
{
	// 성공 메시지
	SUCCESS = 1,
	SUCCESS_CHARACTER_NONE = 2,

	// 실패 메시지
	ERROR_FAIL = 0,
	ERROR_PACKET_VERIFIER = 0x81000000,
	ERROR_PACKET_PROTOCOL,
	ERROR_PACKET_MESSAGE,
	ERROR_PACKET_USER,
	ERROR_DB_FAIL,

	ERROR_CHARACTER_NAME_DUPLICATION,

	ERROR_FAIL_MAX
}
